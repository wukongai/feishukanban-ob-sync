---
created: 2026-05-28T18:30:00
status: handoff-pending
from_project: feishukanban-ob-sync(Claude Code)
to_project: OB(Claudian)
related_handoff:
  - 2026-05-28-task-md-补5字段-handoff.md(原 OB→sync 需求)
  - 2026-05-28-task-md-补5字段-完成回执.md(sync→OB 完成回执)
priority: P1
estimated_effort: 30-40 分钟(含人工飞书改字段 ~5 分钟)
tags:
  - handoff
  - 起手
  - v0.5.0
  - 真机验收
---

# v0.5.0 真机验收起手 — 5 字段闭环端到端测试

## 一句话需求

实施 CC 在 v0.5.0 完成了 sync.py 5 字段(完成质量 / 用时 / 父任务 / 交付正文段 / 用户故事正文段)forward + reverse + 反向建三处代码,dry-run 全通过。**现在需要 OB CC 用真实 task md 配合人工飞书改字段,跑完 OB ↔ 飞书双向真机闭环**,完成后写反向回执 → 触发实施 CC commit + tag + push v0.5.0。

## 背景

详见 [[2026-05-28-task-md-补5字段-handoff.md]](handoff 原文)+ [[2026-05-28-task-md-补5字段-完成回执.md]](实施回执)。本起手 handoff 仅聚焦"真机端到端验收"环节,**只需要 OB CC 跑 SOP + 用户配合飞书 app 手改 5 字段**。

## 前提检查(OB CC 起步先确认)

| # | 检查项 | 怎么验证 |
|---|---|---|
| 1 | 飞书表后台已加 5 字段(完成质量 / 用时 / 父任务 / 交付 / 用户故事) | `feishu-cli bitable field list --base-token Vy8ubUWKbad5u1s8BCJcd1TlnFf --table-id tblIrLY0nU4sCdRC` 看 27 个字段在不在 |
| 2 | OB 端 task 模板已含 5 字段 | `grep -E "quality\|actual_hours\|parent_task\|📦 交付\|👥 用户故事" "/Users/aim5/Documents/OB/03 Resources/素材库/模版/task 模版.md"` 看 5 项都在 |
| 3 | sync.py 工作树是 v0.5.0(已含 5 字段代码) | `grep "v0.5.0" /Users/aim5/Documents/CodingProject/feishukanban-ob-sync/sync.py` 应见多条 v0.5.0 注释 |

任一不通过 → 暂停 + 在本文件加 `## 起步阻塞` section 告诉用户。

## 实施任务

### Step 1:OB CC 建一条测试 task md(用 v0.5.0 新模板)

用 Cmd+P「📝 快记任务」走完整 9 步(测试新模板)或直接照下面 frontmatter 建:

**文件位置**:`/Users/aim5/Documents/OB/04 Inbox/task/2026-05-28-【v0.5.0 验收】5字段闭环测试.md`

**frontmatter**(5 字段都填示例值,parent_task 选一个**已 sync 飞书**的真实老 task):

```yaml
---
priority: P2
status: doing
today: true
today_history: [2026-05-28]
today_source: planned
created: 2026-05-28T18:30:00
due: 2026-06-05
done_date:
category: 技能工具
subcategory:
project_minor: []
adhd_priority: 自由待办
estimate_hours: 1
actual_hours: 0.5          # ⭐ v0.5.0 新字段 #1
efficiency: 高
quality: 高                # ⭐ v0.5.0 新字段 #2
acceptance:
thinking:
resources:
retrospective:
parent_project: "[[06 小工具开发]]"
parent_subproject:
parent_task: "[[2026-05-28-test-反向-subdone-v2]]"   # ⭐ v0.5.0 新字段 #3 — 必须指向已 sync 的真实老 task
parent_inspiration:
日志: "[[journals/2026-05-28]]"
feishu_record:
feishu_url:
iteration_week:
iteration_month:
completion_month:
tags:
  - task
  - v0.5.0-验收-临时
---
```

**正文**(2 个新 H2 段填示例值):

```markdown
# 【v0.5.0 验收】5字段闭环测试

## 📝 执行概述
验证 v0.5.0 task md 5 字段(完成质量 / 用时 / 父任务 / 交付正文段 / 用户故事正文段)forward + reverse 双向同步真机通路。

## 📦 交付
- sync.py v0.5.0 工作树(forward + reverse + 反向建三处实现)
- 飞书表 27 字段(原 22 + v0.5.0 新 5)
- 本测试 task md(可在验收完后删)

## 👥 用户故事
作为本工具维护者 + ADHD 用户,我希望 OB 端「交付」/「用户故事」等正文段能反向从飞书侧拉回,以便不论在飞书 app 还是 OB 端编辑都不丢内容。

## ✅ 验收条件
- forward apply 后飞书 record 5 字段都对(完成质量=高 / 用时=0.5 / 父任务=父 task link / 交付 / 用户故事)
- 飞书 app 手改 5 字段后 reverse apply,OB task md 5 字段都被覆盖

## ✅ 完成标记
- [ ] 【v0.5.0 验收】5字段闭环测试
```

### Step 2:OB CC 跑 forward apply

```bash
python3 /Users/aim5/Documents/CodingProject/feishukanban-ob-sync/sync.py \
    --vault /Users/aim5/Documents/OB \
    --task-md "/Users/aim5/Documents/OB/04 Inbox/task/2026-05-28-【v0.5.0 验收】5字段闭环测试.md" \
    --apply
```

**期望输出**:
- ✅ 飞书 CREATE 成功,新 record_id 回写到 frontmatter `feishu_record / feishu_url`
- ✅ dry-run 输出含 5 字段:`完成质量: 高 / 用时: 0.5h / 父任务: [[2026-05-28-test-反向-subdone-v2]] / 📦 交付: ... / 👥 用户故事: ...`
- ✅ Payload JSON 含 `"完成质量": ["高"]`、`"用时": 0.5`、`"父任务": ["rec..."]`、`"交付": "..."`、`"用户故事": "..."` 5 key

**人工验证**:打开飞书 app(或 base URL)看新建的 record,这 5 字段值都对吗?

⚠️ 如果其中某字段在飞书侧**没显示**(空):
- 「父任务」link 字段没值 → 检查 `2026-05-28-test-反向-subdone-v2.md` 是不是真的有 `feishu_record` frontmatter;没有就换一个已 sync 的父 task
- 「完成质量」select 字段空 → 检查飞书表后台「完成质量」的 enum 选项是不是 ["高", "中", "低"](必须精确匹配)
- 其他字段空 → 看 dry-run 输出有没有报错

### Step 3:【人工】在飞书 app 上手工改这条 task 的 5 字段

> 这一步是**用户必须手动做**的,模拟"用户白天在飞书 app 改字段"的真实场景。OB CC 这步只负责告诉用户该改哪些字段。

打开飞书 app 找到刚 forward 建的 record(标题 `【v0.5.0 验收】5字段闭环测试`),逐字段改:

| 字段 | 原值(OB→飞书) | 改成(人工) |
|---|---|---|
| 完成质量 | 高 | **中** |
| 用时 | 0.5 | **1.2** |
| 父任务 | (原父 task link) | **改换一个**(或多加一个,看 link 字段支持单选还是多选) |
| 交付 | (原 markdown 列表) | **加一段:`\n\n[飞书手改 v0.5.0 测试]`** |
| 用户故事 | (原句) | **替换为**「作为飞书 app 用户,我希望手改后 OB 端能同步看到,以便随时切端办公。」|

改完保存。

### Step 4:OB CC 跑 reverse pull-today --apply

```bash
python3 /Users/aim5/Documents/CodingProject/feishukanban-ob-sync/sync.py \
    --vault /Users/aim5/Documents/OB \
    --pull-today --apply
```

**期望输出**:
- 「计划摘要」里「字段同步」**至少 1 条**有飞书侧字段改动(本测试 task)
- 「字段同步详情」里能看到本 task 的 diff:
  - `• quality: 高 → 中`
  - `• actual_hours: 0.5 → 1.2`
  - `• parent_task: [[原父]] → [[新父]]`(或反之)
  - `• 📑 交付: ... → ... [飞书手改 v0.5.0 测试]`
  - `• 📑 用户故事: 原句 → 「作为飞书 app 用户,...」`
- apply 段输出 `✅ ... + 3 字段 sync + 2 H2 段 sync(today 已对齐)`

**人工验证**:打开 `04 Inbox/task/2026-05-28-【v0.5.0 验收】5字段闭环测试.md` 验证:
- frontmatter `quality / actual_hours / parent_task` 3 字段被改成飞书侧最新值
- 正文 `## 📦 交付` / `## 👥 用户故事` 2 个 H2 段内容被覆盖为飞书侧最新值

### Step 5:【可选】反向建 端到端测试

如果你想顺手验"飞书侧建新 task → OB 端自动建"路径:

1. 在飞书 app 上**直接建一条新 record**(不走 OB):
   - 任务标题:`【v0.5.0 验收】反向建测试`
   - 价值优先级:P2
   - 执行状态:Todo
   - 是否今日:勾选 ✅
   - 完成质量:高
   - 用时:0.3
   - 交付:`从飞书侧直接建测试 v0.5.0`
   - 用户故事:`作为飞书侧用户,我希望我直接建的 task 能反向出现在 OB 端,以便 OB 端 dataview 看到全部今日 task。`
2. 再跑 `python3 ... --pull-today --apply`
3. **期望**:OB 端 `04 Inbox/task/2026-05-28-【v0.5.0 验收】反向建测试.md` 被自动建,模板含 5 字段 frontmatter + 正文 H2 段(本 handoff 默认就跑这步)

### Step 6:清理测试数据

- OB 端删:
  - `04 Inbox/task/2026-05-28-【v0.5.0 验收】5字段闭环测试.md`
  - `04 Inbox/task/2026-05-28-【v0.5.0 验收】反向建测试.md`(如果 Step 5 跑了)
- 飞书 app 端删:
  - 同名的 2 条 record

或者保留作为"v0.5.0 验收痕迹"(看你)。

### Step 7:写反向回执

在本 handoff 同目录创建 `2026-05-28-v0.5.0-真机验收-反向回执.md`,内容简短即可:

```markdown
# v0.5.0 真机验收 — 反向回执

## 一句话回执

Step 1-{2|3|4|5} 全部通过 / Step X 在 [...] 卡住 / 全部失败,详情如下。

## Step 通过情况

- Step 1 建测试 task md:✅/❌
- Step 2 forward apply:✅/❌(飞书侧 5 字段都对吗:Y/N)
- Step 3 人工飞书改字段:✅/❌
- Step 4 reverse pull-today:✅/❌(OB task md 5 字段都被覆盖吗:Y/N)
- Step 5(可选)反向建:✅/❌ / 没跑

## 遇到的问题(如有)

(描述具体问题 + 怎么阻塞了)

## 给实施 CC 的下一步

- ✅ 全通过 → 实施 CC commit + tag + push v0.5.0
- ❌ 有问题 → 在本回执留言 + ping 实施 CC 调试
```

## 沟通约定

- ❓ **测试中遇到问题** → 在本起手 handoff 加 `## 测试问题` section,**保留现场不 cleanup**,告诉用户切回 sync.py CC
- 🚀 **全通过** → 写反向回执 + 一句话告诉用户"切回 sync.py CC 触发 commit"
- ⚠️ **极端情况:apply 把 OB task md 写坏** → 立刻停,告诉用户;OB CC 别自作主张回滚

## 启动指令(给 OB CC)

1. 读本起手 handoff
2. 读 [[2026-05-28-task-md-补5字段-完成回执.md]] 了解 v0.5.0 实施细节(如需理解 sync.py 行为)
3. 走前提检查 1-3
4. 跑 Step 1-7
5. 写反向回执

## 状态变更记录

| 日期 | 状态 | 说明 |
|------|------|------|
| 2026-05-28T18:30 | handoff-pending | sync.py CC 发起,待 OB CC 接手 |
