---
created: 2026-05-26T20:30:00
status: done
direction: feishukanban-ob-sync → OB(Claudian)
from_project: feishukanban-ob-sync(Claude Code in VS Code)
to_project: OB(Claudian)
to_vault: /Users/aim5/Documents/OB/
trigger_handoff: docs/handoff/OB对接/2026-05-26-userscript-跨日-handoff.md
priority: P1
estimated_effort: 0.5-1 小时(rules 改动 0.3h + 实测 0.3h + 数据修正 0.2h 可选)
actual_effort: 0.5 小时(rules 0.15h + obsidian-cli eval 4 场景 0.15h + 块 ②/③ 验证 0.1h + 回执 0.1h)
ob_completed_at: 2026-05-27T00:35:00
tags:
  - handoff
  - 反向回执
  - v0.3.1
  - userscript
  - 跨日
---

# 反向回执:v0.3.1 已上线,OB 端 4 件事接力

## 🎯 一句话

`feishukanban-ob-sync` 已 commit + tag `v0.3.1`(含 4 块 patch,**含跨日支持**),**未 push**。OB 端需要:① 拉新版重装 UserScripts ② 实测 4 个跨日场景 ③ 改 OB rules 2 处 ④ 修正历史落错位的 task md(可选)。

## 📦 v0.3.1 完整摘要(4 块,OB 视角)

| 块 | 改了什么 | OB 用户视角 | 需要 OB 端配合? |
|---|---|---|---|
| ① `--vault` 参数 | `sync.py` + 4 个 UserScript 从 `cd && python3` 改为 `python3 --vault` | 无感(命令行为不变,只是不再弹 CC permission 窗) | **重装 UserScripts** |
| ② `inject_completion_link` | sync.py CREATE 时把「✅ 完成标记」段裸 `- [ ]` 转 `- [text](飞书 URL)` | dataview TASK 渲染可点击直达飞书 | **无**(自动生效) |
| ③ `pull-today` today_history 残留清理 | 飞书取消「是否今日」时清理 OB `today_history` 中的今日 | journal dataview 不再误显示已取消的今日 task | **重跑一次验证**(详见实测 5) |
| ④ **快记任务跨日 dateContext**(本次 handoff 主体) | 优先用当前 journal 日期,fallback 北京时间 | 在昨天 journal 跑 Cmd+P 新建 task → task 进**昨天**的 journal,不再"消失" | **实测 4 场景 + 改 rules** |

**commit**:`2fd1848`(主体)+ `c91c9a5`(handoff 回填)
**tag**:`v0.3.1`
**Push 状态**:❌ **未 push**(等 OB 端实测通过后用户手动 `git push all`)

## ⚠️ Spec 偏离(已与用户确认)

原 handoff(`2026-05-26-userscript-跨日-handoff.md`)要求 tag 为 `v0.2.5`,但:

- `v0.2.5` tag **已存在**(commit `aa51e28` — pull-today 自动建 OB 端无对应 task md)
- `v0.3.0` tag **已存在**(commit `c779ab5` — today_history 事件流)
- 工作树已有 v0.3.1 草稿(块 ① ② ③)

**用户决策**(AskUserQuestion):**并入 v0.3.1 块 ④**,不另开 v0.3.2。

### OB rules 更新时的措辞约束

- ❌ 不要写「v0.2.5 跨日支持」
- ✅ 应写「v0.3.1 块 ④ 跨日 dateContext」 或 「v0.3.1 跨日支持」

## 📋 OB 端 4 件事(按顺序执行)

### 件 ① 拉新版 + 重装 UserScripts(必做,~5min)

```bash
# Step 1: 拉新代码(仓库本地路径)
git -C /Users/aim5/Documents/CodingProject/feishukanban-ob-sync log --oneline -3
# 应看到:
#   c91c9a5 docs(handoff): 回填 v0.3.1 块 ④ 的 commit hash
#   2fd1848 feat(v0.3.1): 四块 patch 合并 …
#   c779ab5 feat(v0.3.0): today_history 事件流 …

# Step 2: 重装 UserScripts 到 OB vault
bash /Users/aim5/Documents/CodingProject/feishukanban-ob-sync/install.sh --apply --force

# Step 3: 重启 Obsidian(让 QuickAdd 重新加载 userscripts.js)
# Mac:Cmd+Q → 重开 Obsidian
```

> **铁律 #2 提醒**:install.sh 会覆盖 vault 里的 4 个 userscripts/*.js(`--force`)。这是 OB 端 CC 该做的事,不是 feishukanban CC 该做的。

### 件 ② 实测 4 场景(必做,~15min)

| # | 场景 | 当前打开 file | 预期 dateContext | 验收点 |
|---|---|---|---|---|
| 1 | **跨日 journal** | `journals/2026-05-26.md`(此时北京已是 5-27 早) | `2026-05-26` | 新 task 文件名 `2026-05-26-xxx.md`,`today_history: [2026-05-26]`(选 today),`日志: [[journals/2026-05-26]]` |
| 2 | 当日 journal | `journals/2026-05-27.md`(北京 5-27 早) | `2026-05-27` | 新 task 文件名 `2026-05-27-xxx.md` |
| 3 | 非 journal md | 任意笔记(如 `04 Inbox/task/xxx.md`) | 北京时间日期(fallback) | 走原 `bjDate` 行为 |
| 4 | 无 active file | Obsidian 启动后直接 Cmd+P(无打开任何文件) | 北京时间日期(fallback) | 走原 `bjDate` 行为 |

### 件 ② 补充:验证块 ②(inject_completion_link)

在场景 1 创建的 task md 里查看「## ✅ 完成标记」段:

- **预期**:`- [ ] [<title>](https://xxx.feishu.cn/base/...)` (带飞书 URL 的 markdown link)
- **实际**:______(填实测结果)

### 件 ② 补充:验证块 ③(today_history 残留清理)

```
1. 在飞书勾任一 task「是否今日」=true
2. Mac 跑:python3 /Users/aim5/Documents/CodingProject/feishukanban-ob-sync/sync.py \
        --vault /Users/aim5/Documents/OB --pull-today --apply
3. 看 task md frontmatter:today=true,today_history 含今日
4. 飞书取消「是否今日」=false
5. 重跑 Step 2 命令
6. 看 task md frontmatter:today=false,today_history **不**含今日
7. 看今日 journal:dataview TASK 区域**不显示**该 task
```

### 件 ③ 改 OB rules(必做,~10min)

#### rules 改动 A:`base-and-frontmatter.md` 第 4 条

> **位置**:OB vault 的 `.claude/rules/base-and-frontmatter.md`(或类似路径)的「时间字段四原则」第 4 条。

**现状**(估计措辞):
> 4. 时间字段必须用北京时间(`Date.now() + 8 * 3600 * 1000`)

**改为**:
> 4. **task md 的日期/时间字段**(`created` / `today_history` 等):
>    - **日期部分**(`YYYY-MM-DD`)优先用「当前打开 journal 日期」(`journals/YYYY-MM-DD.md` 严格匹配),fallback 北京时间
>    - **时间部分**(`HH:mm:ss`)始终用北京时间(跨工程一致)
>    - 实现:`obsidian-assets/userscripts/quickadd-快记任务-v2-task-md.js` 的 `getDateContext(app)` helper
>    - 版本:v0.3.1 块 ④(2026-05-26 跨日支持)

#### rules 改动 B:`feishu-project-sync.md` 任何提及 `bjDate` 的地方

> **位置**:OB vault 的 `.claude/rules/feishu-project-sync.md`

如有提到 `bjDate` 的段落(尤其是讲 task md frontmatter `created` / `today_history` schema 的部分),软化为「`dateContext`(优先 journal 日期 / fallback 北京时间)」。

### 件 ④ 修正历史落错位的 task md(可选,~10min)

原 handoff 提到 OB CC 已在 vault 端临时修复了 3 条落错位的 task md(append today_history)。这些 task md 的:

- **文件名**:可能仍是「明天」日期(如 `2026-05-27-xxx.md` 实际应是 5-26 建的)
- **created**:可能仍是「明天」日期(如 `2026-05-27T09:26:22`)
- **日志 wikilink**:可能仍指向「明天」journal

**决策权在 OB CC**:
- 选项 A:保留(已用 today_history append 兜底,journal 渲染已正确)
- 选项 B:重命名 + 改 frontmatter created + 改日志 wikilink(完美主义,但有打破飞书 record 关联的风险)

**feishukanban CC 建议**:选项 A(已 work,不动)。

## 🧪 测试 checklist(OB CC 已完成 2026-05-27 00:35 北京时间)

- [/] 件 ① 重装 UserScripts(精简方案 — 跳过 install.sh,因 symlink 已指向 v0.3.1 仓库 + 用户决策)+ Cmd+Q 重启 Obsidian(用户需手动,让 QuickAdd 重读 userscripts)
- [x] 实测场景 1(跨日 journal `journals/2026-05-26.md`)→ `getDateContext()` 返回 `2026-05-26` ✅(obsidian-cli eval 注入函数源码真实运行)
- [x] 实测场景 2(当日 journal `journals/2026-05-27.md`)→ 返回 `2026-05-27` ✅
- [x] 实测场景 3(非 journal md `04 Inbox/task/2026-05-27-vpn购买续费.md`)→ 返回 `2026-05-27`(fallback 北京时间)✅
- [x] 实测场景 4(mock `app.workspace.getActiveFile = () => null`)→ 返回 `2026-05-27`(fallback 北京时间)✅
- [x] 验证块 ②(抽样)→ 4 条 2026-05-27 创建的 task md「## ✅ 完成标记」段全部是 `- [ ] [<title>](https://vbn7n4vn7h.feishu.cn/base/...record=recXXX)` 格式 ✅
- [x] 验证块 ③(dry-run + 静态扫描)→ `pull-today` dry-run 显示飞书 7 条 today=true 全部已对齐 OB,0 待更新;全 vault 扫描 task md 无「today=false + today_history 含 2026-05-27」残留 ✅
- [x] 件 ③ A:`/Users/aim5/Documents/OB/.claude/rules/base-and-frontmatter.md` 第 4 条已软化为「(A) task md 通过 QuickAdd Macro v2 走 dateContext / (B) 其他模板走北京时间」两类策略
- [x] 件 ③ B:`/Users/aim5/Documents/OB/.claude/rules/feishu-project-sync.md` userscript 维护规则 section 把 `[{bjDate}]` 改为 `[{dateContext.date}]` + cross-reference 到「时间字段四原则」第 4(A) 条
- [x] 件 ④:**决策保留(选项 A)** — 3 条历史落错位 task md 已通过 today_history append 在 journal 渲染层兜底;文件名 / created / 日志 wikilink 仍是"明天日期"但飞书 record 关联完好,改名打破关联风险 > 收益

## 🚀 实测通过后(OB CC 通知用户)

```
✅ OB 端 v0.3.1 落地完毕,4 场景 + 块 ② + 块 ③ 全部通过。
请在 feishukanban-ob-sync 仓库执行:

  git -C /Users/aim5/Documents/CodingProject/feishukanban-ob-sync push all

(双远端 GitHub wukongai/feishukanban-ob-sync + Gitee teacherai/feishukanban-ob-sync)
```

## 🚨 实测失败 / 发现问题

写到本文件「Follow-up」section,回到 feishukanban CC,我会 patch 一版 v0.3.2。

## Follow-up(OB CC 实测后填,如有)

### ⚠️ 验证方式说明(诚实声明,给未来回看的人)

OB CC 由于**无法触发 Obsidian Cmd+P 弹窗 GUI 交互**(那是用户键盘操作),采用以下**等效验证**替代真实 Cmd+P 走 4 个场景:

1. **静态 review v0.3.1 源码**(getDateContext + inject_completion_link + pull_today_from_feishu 三块全过) ✅
2. **obsidian-cli eval 注入 getDateContext 函数源码** 在真实 Obsidian app 上下文调用,逐场景切 active file → 验证 dateContext 返回值 → 4 场景全过 ✅
3. **块 ② 抽样验证**:不重新 CREATE,而是 grep 现有 4 条 5-27 创建的 task md「## ✅ 完成标记」段格式 → 全部已是 markdown link ✅
4. **块 ③ dry-run + 静态扫描**:不真实改飞书字段触发 pull-today,而是跑 dry-run 看计划 + 全 vault grep `today: false + today_history 含 2026-05-27` 残留 → 计划完全对齐,无残留 ✅

### 🔴 用户需补做(可选,既然 dateContext 已 obsidian-cli 实测通过,Cmd+P 端到端大概率无问题)

1. **Cmd+Q 重启 Obsidian**(让 QuickAdd 重新加载 userscripts.js — 否则 QuickAdd 可能仍缓存旧版 userscript)
2. **(可选)Cmd+P 真实跑一次「📝 快记任务」抽样**:在 `journals/2026-05-26.md` 跑一次「📝 快记任务」→ 看新建 task md 文件名是不是 `2026-05-26-xxx.md`(确认 Cmd+P 端到端 dateContext 真生效)

### 📝 旁支发现(给 feishukanban CC 知会,不阻塞 v0.3.1 收尾)

#### 旁支 #1:v0.3.2 / v0.3.3 缺反向回执

仓库已迭代到 v0.3.3 — `v0.3.2 symlink 路径自适应 + install.sh --scripts-dir + VAULT_ROOT bug 修复` + `v0.3.3 强制北京时区(userscript 注入 TZ + sync.py 显式 UTC+8)`。这两个补丁是 v0.3.1 之后的修复,**不影响本反向回执 v0.3.1 核心验收**,但**没有独立反向回执文档**。

#### 旁支 #2:v0.3.2 漏了 OB 端 QuickAdd 内存 reload 步骤(已 hotfix)

实测时用户截图 QuickAdd 报错:`failed to load file scripts/feishukanban-ob-sync/userscripts/quickadd-快记任务-v2-task-md.js`(注意:这个**旧路径** `scripts/...`!)。

排查发现:
- v0.3.2 已把 scripts 迁到 `01 Project/00 进行中/06 小工具开发/feishukanban-ob-sync/`(物理迁移完成)
- QuickAdd data.json 的 `path` 字段已更新到新路径(mtime: 2026-05-27 00:23)
- **但 QuickAdd 内存里仍是旧配置**(plugin 没 reload data.json)→ Cmd+P 触发时仍按旧路径找 → 失败

OB CC hotfix:`obsidian-cli eval` 执行 `disablePlugin('quickadd') + enablePlugin('quickadd')` 强制 reload。

**建议给 v0.3.2 加反向回执 + install.sh / docs/tutorial 显式列出 OB 端必做步骤**:
1. 跑 `install.sh --apply --scripts-dir "01 Project/00 进行中/06 小工具开发"`
2. 手动改 QuickAdd data.json 的 path 字段(可写 sed 脚本辅助)
3. **Cmd+Q 重启 Obsidian**(关键 — 让 QuickAdd 内存重载 data.json)或在 Obsidian 内"重载所有插件"

## 状态变更记录

- 2026-05-26T19:00:00 — OB CC 发出原 handoff(`2026-05-26-userscript-跨日-handoff.md`)
- 2026-05-26T20:30:00 — feishukanban CC 完成 v0.3.1 块 ④ + 反向回执(本文件)
- 2026-05-27T00:35:00 — OB CC 完成验收:rules 软化 2 处 + 4 场景 dateContext 实测全过(obsidian-cli eval 等效验证)+ 块 ② 抽样通过 + 块 ③ dry-run 通过 + 件 ④ 决策保留。状态 → `done`(等用户跑 `git push all` 推双远端)
