---
title: 起手指令 — skill 约束补强(task 明细单条 + 交付绝对路径)
type: handoff-kickoff
status: 待执行
created: 2026-06-11
target_project: feishukanban-ob-sync
target_files:
  - .claude/skills/同步任务到飞书/SKILL.md
  - sync.py
related:
  - "[[2026-06-11-skill约束补强-task明细单条+交付绝对路径-handoff]]"
---

# 起手指令(直接 paste 给 feishukanban-ob-sync 项目的 Claude 会话)

> **怎么用**:在 `feishukanban-ob-sync/` 项目里新开一个 Claude Code 会话(cwd 必须是该项目根),把下面 `---` 之间的整段 paste 进去就行。Claude 会读 handoff 文档 → 出实施 plan → dry-run 改动 → 等你批准 → apply。

---

【来自内容工厂会话的 handoff】2026-06-11 在 Content-factory 调用 `/同步任务到飞书` skill 同步一条 task(record `recvlV5IDsBZ5B`)时连续 apply 了 3 次,根因是 skill 文档里**两个已有约束 Claude 没遵守**,需要文档侧加强 + 工具侧拦截。

**任务**:阅读 handoff 文档,落地文档补强 + (可选)工具侧硬拦截,写反向回执。

**handoff 路径**:
`docs/handoff/OB对接/2026-06-11-skill约束补强-task明细单条+交付绝对路径-handoff.md`

**两个错误一句话**:
1. 执行明细同日拆多行 → sync.py 按日期合并只识首条,其他丢失
2. 交付段用相对路径(`docs/research/xxx`)→ 飞书侧无归属,OB 双链断链

**强约束诉求**:
- 文档侧:SKILL.md 加反例块 + 「Claude 常见错误自检」段 + checklist 补 2 条
- 工具侧(可选):sync.py 加 `--strict-delivery-paths` / `--strict-detail-one-per-day` flag,dry-run 时强制警告
- 都要让**下次 Claude 在 dry-run 阶段就能被警示**,不依赖 LLM 自觉

**执行流程**:
1. 先 Read handoff 主文档全文 — 内有错误案例、自我反思、A/B/C 三档建议、验证标准
2. 出实施 plan(给我看哪几个文件改、改什么、按什么顺序)
3. **特别注意**:本仓库 SKILL.md 真相源是 `/Users/aim5/.claude/skills/同步任务到飞书/SKILL.md`(全机器共享),仓库内 `.claude/skills/同步任务到飞书/SKILL.md` 是 symlink 还是副本要先 `ls -la` 确认。改 SKILL.md 改哪份要先想清楚
4. dry-run 改动给我看 → 等批准 → apply
5. 改动落地后写反向回执:`docs/handoff/OB对接/2026-06-11-skill约束补强-反向回执.md`,说明:
   - 采纳 / 部分采纳 / 暂缓的项各是什么 + 原因
   - SKILL.md 版本号 + commit hash
   - sync.py 版本号(如果加了工具拦截)
   - 留给内容工厂会话(我)同步到本项目 memory 的关键变更摘要

**红线**(自动遵守,不必每次问):
- 改 sync.py 必须本地跑过(用之前出错的 task `recvlV5IDsBZ5B` 的 md 副本做回归)
- 不破坏现有 `--apply` / `--strict-soft-sections` 等 flag 的兼容性
- 不直接 push 远程,等本地确认后我手动 push

开始干。

---

## 配套提示(可选 paste)

如果对方 Claude 问"task md 副本在哪做回归测试"→ 回复:

> 把 `/Users/aim5/Documents/OB/04 Inbox/task/2026-06-08-【内容工厂】调研体验开源项目:yikart_AitoEarn,自媒体开源项目.md` cp 一份到 `tests/fixtures/regression-2026-06-11-delivery-paths.md`(去掉 feishu_record / feishu_url 避免误推),做成测试用例。

如果对方 Claude 问"工具侧硬拦截要不要做"→ 回复:

> 先文档补强(A 段),工具拦截(B 段)看你工作量 — 如果 1 小时内能搞,顺手做;不能就只做文档,工具侧拆成单独 backlog。
