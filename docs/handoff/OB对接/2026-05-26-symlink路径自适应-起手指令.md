---
created: 2026-05-26T20:45:00
type: 起手指令
related_handoff: "[[2026-05-26-symlink路径自适应-handoff.md]]"
---

# 起手指令 — symlink 路径自适应 vault 落地

> 用户从独立 CC (feishukanban-ob-sync) 移交过来。打开 `/Users/aim5/Documents/OB/` 启动 OB CC (Claudian),粘贴下方代码块作为第一句指令。

```
@OB CC

独立 CC (feishukanban-ob-sync) 移交一个任务给你:

read 这份 handoff:
/Users/aim5/Documents/CodingProject/feishukanban-ob-sync/docs/handoff/OB对接/2026-05-26-symlink路径自适应-handoff.md

任务一句话:独立仓库 v0.3.2 已完成代码自适应(userscript 用 __filename 推导 sync.py 路径,
install.sh 加 --scripts-dir flag)。请你按 handoff Step 1-9 顺序执行:

1. 决定新路径(你判断,候选见 handoff,用户原话"放在 ob 的小工具下统一管理")
2. 备份旧目录 /Users/aim5/Documents/OB/scripts/feishukanban-ob-sync/(改名加 .backup-时间戳)
3. 跑 install.sh --scripts-dir <你选的路径> --apply
4. 手动 ln config.yaml symlink 到新位置
5. 改 QuickAdd data.json 的 4 个 choice path 字段(handoff 给了 jq 命令)
6. Cmd+Q 重启 Obsidian
7. 测 4 个 Cmd+P 命令(handoff 测试用例表)
8. 改独立仓库 .claude/CLAUDE.md line 178-184 的 vault 路径段(改完不要 commit,留 working tree)
9. 写反向回执:2026-05-26-symlink路径自适应-反向回执.md

完成后:
- 反向回执填:选的路径 / 4 个测试结果 / 偏离点
- 通知用户 vault 端落地完成,等独立 CC commit + push v0.3.2

注意:
- 不要 commit 独立仓库代码(铁律 #7 重申)
- 不要 push 远端
- 如有任何步骤失败/疑问,写在反向回执「偏离点」section,带回独立 CC 决策
```
